<% data.each do |project_name, states| %>
=== <%= project_name %>
Sprint Day *X* of 10
<% states.each do |state, issues| %>
*<%= state || "Unknown" %>*:
<% issues.each do |issue| %>
- <%= issue[:url] %> (<%= issue[:title] %>)<%= " (#{issue[:days]} days)" if issue[:days] %><%= " (@#{issue[:slack_username]})" if issue[:slack_username] %><% end %>
<% end %>
Stories >= 3pt in Buildable:
<% end %>
